# Package marker for inbox assistant runtime.
# Note: LLM observability is initialized in src/main.py when the RT service starts,
# not here, to avoid requiring heavy dependencies for CLI-only usage.
